{
  "name": "wa-absensi-guru",
  "private": true,
  "workspaces": [
    "shared",
    "web"
  ],
  "scripts": {
    "web:dev": "npm -w web run dev",
    "web:build": "npm -w web run build",
    "db:migrate": "npm -w web run db:migrate",
    "db:seed": "npm -w web run db:seed"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "openai": "7.4.0",
    "typebox": "^1.3.13"
  }
}
